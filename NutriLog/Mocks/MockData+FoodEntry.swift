import Foundation

extension MockData {
  
    static let foodEntries = [
        FoodEntry(food: banana, servingSize: 100, mealType: .breakfast, date: Date()),
        FoodEntry(food: banana, servingSize: 100, mealType: .lunch, date: Date()),
        FoodEntry(food: proteinFood, servingSize: 150, mealType: .dinner, date: Date())
    ]
}
