import SwiftUI
import LocalAuthentication

struct LoginView: View {
    var onLoginSuccess: () -> Void
    
    var body: some View {
        Image("image")
        
        
    }
}

#Preview {
    LoginView(onLoginSuccess: {})
}
